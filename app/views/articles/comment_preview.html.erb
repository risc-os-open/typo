<div class="comment-content">
  <div class="citation">
    <%= gravatar_tag(@comment.email) if this_blog.use_gravatar and @comment.email.present? %>
    <cite><strong><%=h @comment[:author] %></strong></cite> <%= _("is about to say") %>:
  </div>

  <%= @comment.html %>
</div>
