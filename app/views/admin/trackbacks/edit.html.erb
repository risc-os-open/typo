  <% @page_heading = _('Trackbacks for %s', link_to(@article.title, controller: 'admin/content', action: 'show', id: @article)).html_safe() %>

  <%= form_tag :action=>"edit", :id => @trackback.id do %>

  <div class="form">
    <h3><%= _("Editing trackback")%></h3>
    <%= render partial: "form" %>
  </div>

  <div id="operations">
    <%= save %> or <%= cancel %>
  </div>

  <% end %>
