  <% @page_heading = _('Feedback') %>

  <p><%= _("Are you sure you want to delete this %s?", @feedback.type)%></p>
  <%= form_tag :action => "destroy", :id => @feedback.id do %>
  <%= confirm_delete(_("Delete this %s", @feedback.type)) %>
  <% end %>
