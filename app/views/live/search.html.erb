<% if !@search.to_s.blank? %>
  <br />
  <h4>Searched for <em>"<%=h @search %>"</em></h4>
  <ul>
  <% for article in @articles.to_a %>
    <li><%= link_to_permalink article,h(article.title) %></li>
  <% end %>
	</ul>
<% else %>
<% end %>
